/** External id stored on imported Teller rows from Hassle Free AC. */
export const HFAC_EXTERNAL_SOURCE = "hfac";

/** Legacy imports before HFAC naming cleanup. */
export const LEGACY_HFAC_EXTERNAL_SOURCES = ["hfac", "quoter"] as const;

export const HFAC_INTEGRATION_MODULE = "hfac";

/** Legacy module id from early builds. */
export const LEGACY_INTEGRATION_MODULES = ["hfac", "quoter"] as const;
